{
  "name": "playwright-shared",
  "description": "One Playwright MCP daemon per machine instead of one per session — every session gets its own tabs, logins flow between them automatically, and the profile-lock contention between parallel sessions disappears. Registers the HTTP server, keeps the daemon up from a SessionStart hook, and installs a login-time service.",
  "version": "1.0.0",
  "author": { "name": "young1ll", "url": "https://github.com/young1ll" },
  "homepage": "https://github.com/young1ll/done/tree/main/plugins/playwright-shared",
  "repository": "https://github.com/young1ll/done",
  "license": "MIT",
  "keywords": ["playwright", "browser", "mcp", "parallel", "multi-agent", "daemon"]
}
