{
  "description": "Keep the shared Playwright daemon up so every session's browser tools connect on the first try.",
  "hooks": {
    "SessionStart": [
      { "matcher": "startup|resume", "hooks": [ { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/ensure.sh\"", "timeout": 10 } ] }
    ]
  }
}
